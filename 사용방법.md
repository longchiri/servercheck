# iDRAC Viewer — Mac / Windows 통합 버전

Dell iDRAC Redfish API 를 이용해 서버의 HW / FW / BIOS 정보를 GUI 로 조회합니다.
PySide6(Qt) 기반이라 macOS 와 Windows 가 **같은 소스코드** 로 동작합니다.

## 파일 구성

| 파일 | 설명 |
|---|---|
| `dell_viewer.py` | 앱 본체 (실행 진입점, macOS/Windows 공통) |
| `build_mac.sh` | macOS .app 빌드 스크립트 |
| `build_win.bat` | Windows .exe 빌드 스크립트 |
| `사용방법.md` | 이 문서 |

> 기존 `dell_app.py`, `build_mac_app.sh` 는 Tkinter 버전이라 macOS Tk 8.5 환경에서 빈 화면으로 보이는 문제가 있었습니다. 새 PySide6 버전(`dell_viewer.py`)을 쓰세요.

## macOS 에서 .app 만들기

```bash
cd ~/Documents/Claude/Projects/델\ 관리시스템
chmod +x build_mac.sh
./build_mac.sh
```

생성 결과: `dist/iDRAC Viewer.app`
열기: `open "dist/iDRAC Viewer.app"`

> "확인되지 않은 개발자" 경고가 뜨면 Finder 에서 앱을 **우클릭 → 열기**.
> 또는 터미널에서: `xattr -cr "dist/iDRAC Viewer.app"`

## Windows 에서 .exe 만들기

1. Windows 10/11 PC 에 Python 3.11 이상 설치
   - https://www.python.org/downloads/windows/
   - 설치 시 **Add Python to PATH** 체크 필수
2. 이 폴더 전체를 Windows PC 로 복사 (USB, 메일, 클라우드 등)
3. `build_win.bat` **더블클릭**
4. 검은 창에서 자동으로 패키지 설치 + 빌드 진행 (수 분 소요)
5. 생성 결과: `dist\iDRAC_Viewer.exe`
6. .exe 파일은 어디든 옮겨도 단독 실행 가능 (포터블)

## 사용법

1. 앱 실행
2. **iDRAC IP / Username / Password** 입력 (Username 기본값 `root`)
3. **옵션** 선택:
   - HW (하드웨어 상태)
   - FW (펌웨어 정보)
   - BIOS (BIOS 설정 값)
   - 전체 (HW + FW + BIOS 한 번에)
4. **실행** 클릭 → 결과창에 표시
5. 필요 시 **엑셀로 저장** / **저장 (TXT)** / **복사**

## 주요 기능

- **HW / FW / BIOS 다중 선택** — 체크박스로 자유 조합
- **모든 항목 표시 토글** — BIOS 전체 속성(150~475개) 그룹별로 / FW 전체 컴포넌트
- **5g 모드** — 운영자가 자주 보는 핵심 BIOS 항목만 추린 프리셋
- **🔍 키 필터** — 직접 키워드로 원하는 항목만 추출 (아래 별도 섹션)
- **보기 좋게 토글** — 색상/표 형식 ↔ 평문 전환
- **폰트 크기 조절** (8–24pt)
- **샘플 보기** — 실제 서버 없이 데모 데이터로 동작 확인
- **엑셀 누적 저장** — 같은 .xlsx 파일을 반복 선택하면 서버별로 시트가 추가됨
  (시트 이름은 Service Tag 기준)
- **비밀번호 보기** 체크박스
- **친절한 에러 메시지** — 인증/네트워크/타임아웃 구분
- **백그라운드 스레드** — 조회 중에도 창이 멈추지 않음
- **마지막 입력값 기억** — 다음 실행 시 IP/사용자/폰트/옵션/필터가 자동 복원

## 🔍 키 필터 사용법

결과창 위 "키 필터" 입력칸에 키워드를 넣으면 매칭되는 BIOS 속성/FW 컴포넌트만 표시됩니다.

| 입력 예 | 결과 |
|---|---|
| `ProcTurbo` | `ProcTurboMode` 같이 부분 일치하는 속성만 |
| `Proc*` | `Proc` 로 시작하는 모든 속성 |
| `Mem, Boot, Pxe` | Mem/Boot/Pxe 부분 일치하는 속성 모두 |
| `Numa*, Cxl*` | Numa/Cxl 시작 속성 |
| `NIC, iDRAC` | NIC와 iDRAC 펌웨어 컴포넌트만 |
| `BIOS, PERC` | BIOS / PERC 펌웨어만 |
| `Disk*` | 모든 디스크 펌웨어 |
| (비움) | 필터 OFF — 기본 동작 |

규칙:
- 콤마 / 공백으로 여러 키워드 입력
- 대소문자 무시
- 부분 일치 (예: `Proc` 입력 시 `Processor` 도 매칭)
- `*` 와일드카드 지원
- **show_all 켜고 사용하면 가장 잘 동작함** (모든 속성 중에서 찾으므로)
- BIOS / FW 양쪽 모두에 동시 적용
- 마지막 입력은 자동 저장되어 다음 실행 시 복원

## 지원 모델

표준 Redfish API 를 쓰는 모든 Dell PowerEdge iDRAC 모델 지원 (자동 감지):
- **R6515 / R6525 / R6615 / R6625** (1U AMD)
- **R7515 / R7525 / R7615 / R7625** (2U AMD)
- **R640 / R740 / R740xd / R840 / R940** (Intel 14G)
- **R650 / R750 / R750xs / R7515 등** (Intel 15G)
- **R660 / R760 / R760xa 등** (Intel 16G)
- **MX740c / MX750c / MX760c** (블레이드)
- 그 외 iDRAC 8/9 탑재 서버 전부

## 여러 서버를 한 엑셀에 모으기

1. 서버 1 IP/계정 → **실행** → **엑셀로 저장** → 새 파일 만들기
2. 서버 2 IP 만 변경 → **실행** → **엑셀로 저장** → **같은 파일 선택**
   → 시트가 추가됨 (예: `ABC1234_HW`, `DEF5678_HW`)
3. 반복하면 한 파일에 서버별 시트가 차곡차곡 쌓임

> Windows 의 파일 다이얼로그에서 같은 파일을 선택하면 "Replace?" 경고가 뜰 수 있는데, **Replace** 를 눌러도 실제로는 덮어쓰지 않고 시트만 추가됩니다.

## 시스템 요구사항

- **macOS**: 빌드 시 Homebrew Python 3.13 권장 (시스템 Python 3 도 가능)
- **Windows**: Python 3.11 이상
- 빌드된 결과물(.app / .exe) 실행에는 Python 설치 **불필요**

## 문제 해결

| 증상 | 해결 |
|---|---|
| macOS 에서 "손상됨" 경고 | `xattr -cr "dist/iDRAC Viewer.app"` |
| Windows 에서 SmartScreen 경고 | "추가 정보" → "실행" 클릭 |
| 연결 시간 초과 | iDRAC IP / 방화벽 / VPN 확인 |
| 인증 실패 | 사용자/비밀번호 재확인 |
| 빌드 시 PySide6 설치 오류 | Python 버전이 3.9 미만이면 안 됨. 3.11 이상 권장 |
